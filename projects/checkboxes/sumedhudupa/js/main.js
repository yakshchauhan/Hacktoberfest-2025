document.addEventListener('DOMContentLoaded', () => {
    const checkbox = document.getElementById('rotatingCheckbox');
    const checkboxBox = document.querySelector('.checkbox-box');

    // Optional: Add click sound or haptic feedback here
    checkbox.addEventListener('change', () => {
        // Animation is handled by CSS
        // You can add additional effects here if needed
    });
});
